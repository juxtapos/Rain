define(["{{application_name}}/custom_lib"], function() {
  function init() {
    alert("Component initilized");
  }
  
  function load() {}
  
  function start() {}
  
  function pause() {}
  
  function stop() {}
  
  function dispose() {}
  
  return {
    init : init,
    load : load,
    start : start,
    pause : pause,
    stop : stop,
    dispose : dispose
  };
});